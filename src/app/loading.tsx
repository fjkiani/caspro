import AppLoading from '@/components/ui/AppLoading';

/** Shown during App Router navigations for every route segment without its own `loading.tsx`. */
export default function RootLoading() {
  return <AppLoading />;
}
