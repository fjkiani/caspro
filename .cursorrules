---
alwaysApply: true
---
instructions
During you interaction with the user, if you find anything reusable in this project (e.g. version of a library, model name), especially about a fix to a mistake you made or a correction you received, you should take note in the Lessons section in the .cursorrules file so you will not make the same mistake again.

You should also use the .cursorrules file as a scratchpad to organize your thoughts. Especially when you receive a new task, you should first review the content of the scratchpad, clear old different task if necessary, first explain the task, and plan the steps you need to take to complete the task. You can use todo markers to indicate the progress, e.g. [X] Task 1 [ ] Task 2

Also update the progress of the task in the Scratchpad when you finish a subtask. Especially when you finished a milestone, it will help to improve your depth of task accomplishment to use the scratchpad to reflect and plan. The goal is to help you maintain a big picture as well as the progress of the task. Always refer to the Scratchpad when you plan the next step.

Tools
Note all the tools are in python. So in the case you need to do batch processing, you can always consult the python files and write your own script.

Screenshot Verification
The screenshot verification workflow allows you to capture screenshots of web pages and verify their appearance using LLMs. The following tools are available:

Screenshot Capture:
venv/bin/python tools/screenshot_utils.py URL [--output OUTPUT] [--width WIDTH] [--height HEIGHT]
LLM Verification with Images:
venv/bin/python tools/llm_api.py --prompt "Your verification question" --provider {openai|anthropic} --image path/to/screenshot.png
Example workflow:

from screenshot_utils import take_screenshot_sync
from llm_api import query_llm

# Take a screenshot
screenshot_path = take_screenshot_sync('https://example.com', 'screenshot.png')

# Verify with LLM
response = query_llm(
    "What is the background color and title of this webpage?",
    provider="openai",  # or "anthropic"
    image_path=screenshot_path
)
print(response)
LLM
You always have an LLM at your side to help you with the task. For simple tasks, you could invoke the LLM by running the following command:

venv/bin/python ./tools/llm_api.py --prompt "What is the capital of France?" --provider "anthropic"
The LLM API supports multiple providers:

OpenAI (default, model: gpt-4o)
Azure OpenAI (model: configured via AZURE_OPENAI_MODEL_DEPLOYMENT in .env file, defaults to gpt-4o-ms)
DeepSeek (model: deepseek-chat)
Anthropic (model: claude-3-sonnet-20240229)
Gemini (model: gemini-1.5-pro)
Local LLM (model: Qwen/Qwen2.5-32B-Instruct-AWQ)
But usually it's a better idea to check the content of the file and use the APIs in the tools/llm_api.py file to invoke the LLM if needed.

Web browser
You could use the tools/web_scraper.py file to scrape the web.

venv/bin/python ./tools/web_scraper.py --max-concurrent 3 URL1 URL2 URL3
This will output the content of the web pages.

Search engine
You could use the tools/search_engine.py file to search the web.

venv/bin/python ./tools/search_engine.py "your search keywords"
This will output the search results in the following format:

URL: https://example.com
Title: This is the title of the search result
Snippet: This is a snippet of the search result
If needed, you can further use the web_scraper.py file to scrape the web page content.

Cursor learned
=======


Scratchpad
==========
## TASK: Strategic Data Migration from src2 to main codebase
**GOAL**: Migrate Oracle, Forge, Boltz capabilities and slide decks while maintaining modularity

### Current State Analysis:
- [X] Identified src2/data structure with existing content
- [X] Found existing endpoints in discriminativeAIContent.ts, forgeGenerativeAPIs.ts, etc.
- [ ] MISTAKE: Started hard-coding instead of using existing data structures
- [ ] Need to understand what already exists vs what needs migration

### Next Steps:
1. [X] Audit existing src2/data files to understand current structure
2. [ ] Create migration plan that REUSES existing data, doesn't recreate
3. [ ] Identify gaps between src2 and main codebase
4. [ ] Create modular migration strategy

### AUDIT FINDINGS:
**Existing Data Structure in src2/data:**
- discriminativeAIContent.ts - Complete Oracle endpoints with REAL metrics
- forgeGenerativeAPIs.ts - Complete Forge endpoints with simulations
- forgeCapabilities.ts - Forge multi-modal capabilities
- oracleCapabilities.ts - Oracle multi-modal predictions
- dossierSummaries.ts - API result summaries
- multiModalContexts.ts - Business context for biotech/clinical
- Slide decks in decks/ folder (oracle-showcase.tsx, forge-showcase.tsx, etc.)
- Use cases in useCases/ (discriminative.ts, generative.ts)

**MIGRATION STRATEGY:**
Phase 1: Direct file migration (copy + adapt imports) ✅
Phase 2: Create adapter layer for component integration ✅
Phase 3: Integrate with existing homepage components

### STRATEGIC MISSION EXECUTION:
**Phase 1 - DATA FOUNDATION ✅:**
- ✅ Migrated Oracle discriminative intelligence (95.7% AUROC, 73% VUS resolution)
- ✅ Migrated Forge generative capabilities (70% Pfam-hit rate, AlphaFold 3 validation)
- ✅ Created modular adapter system for component integration
- ✅ Eliminated component dependencies - pure data architecture

**Phase 2 - PRODUCT DEMONSTRATION ✅:**
- ✅ Oracle product page (/products/oracle) - WORKING with real metrics
- ✅ Homepage integration component (OracleShowcase) using adapters
- ✅ Fixed all runtime errors - EssentialityChart resolved

**Phase 3 - BILLION DOLLAR EXECUTION (CURRENT):**
- [ ] Complete remaining data migrations (Boltz, Command Center, Use Cases)
- [ ] Transform homepage with drug development doctrine 
- [ ] Replace static demos with interactive simulations using migrated data
- [ ] Create slide composition system for investor presentations
- [ ] Integrate with existing clinical dossier and trial matching systems

**STRATEGIC OBJECTIVE:** Transform CrisPRO.ai homepage into a LIVING DEMONSTRATION of our drug development transformation doctrine - replacing $2.6B gambling with mathematical certainty through Oracle + Forge + Boltz intelligence engines.

### Lessons Learned:
- Don't hard-code when data structures already exist
- Always audit existing code before creating new structures
- Take incremental steps, not massive code edits


// New structure - following existing patterns but enhanced
src/data/
├── products/                    # Core product capabilities
│   ├── oracle/                 # Discriminative AI content
│   │   ├── capabilities.ts     # From oracleCapabilities.ts
│   │   ├── content.ts          # From oracleContent.ts  
│   │   ├── sample-data.ts      # From discriminativeAISampleData.ts
│   │   └── use-cases.ts        # From useCases/discriminative.ts
│   ├── forge/                  # Generative AI content
│   │   ├── capabilities.ts     # From forgeCapabilities.ts
│   │   ├── content.ts          # From forgeContent.ts
│   │   ├── apis.ts             # From forgeGenerativeAPIs.ts
│   │   └── use-cases.ts        # From useCases/generative.ts
│   ├── boltz/                  # Structural validation
│   │   └── content.ts          # From boltzContent.ts
│   └── command-center/         # Orchestration
│       └── content.ts          # From commandCenterContent.ts
├── slides/                     # Pre-built slide decks
│   ├── decks/                  # From src2/data/decks/
│   ├── templates/              # Slide templates
│   └── compositions/           # Dynamic slide compositions
├── use-cases/                  # Integrated use cases
│   ├── multiple-myeloma/       # From multipleMyeloma*.ts
│   ├── breast-cancer/          # Extracted from content
│   └── crispr-therapy/         # Extracted from content
├── contexts/                   # Multi-modal contexts
│   └── multi-modal.ts          # From multiModalContexts.ts
└── adapters/                   # Content-component adapters
    ├── oracle-adapter.ts       # Component binding
    ├── forge-adapter.ts        # Component binding
    └── slide-composer.ts       # Slide composition
